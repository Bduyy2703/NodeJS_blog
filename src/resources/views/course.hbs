<div class="page-container">
    <div class="mt-4 course-container">
        <div class="row">
            <div class="col-lg-3">
                <button class="study-now-btn">Study Now</button>
            </div>
            <div class="col-lg-9">
                <h2 class="course-title">{{course.name}}</h2> <!-- Hiển thị tên của khóa học -->
                <iframe class="course-video" width="560" height="315" src="https://www.youtube.com/embed/{{course.videoID}}?si=631fWqHoXM_9AZmS" 
                    title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
                    referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>
                <p class="course-description">{{course.description}}</p> <!-- Hiển thị mô tả của khóa học -->
            </div>
        </div>
    </div>
</div>

<style>
  /* Đặt form ra giữa màn hình */
  .page-container {
    font-family: 'Poppins', sans-serif;

    display: flex;
    justify-content: center; /* Căn giữa theo chiều ngang */
    align-items: center; /* Căn giữa theo chiều dọc */
    min-height: 100vh; /* Đảm bảo chiều cao tối thiểu là 100% chiều cao viewport */
    padding: 10px;
    color: #333;
    width: 100%; /* Hoặc một giá trị cụ thể như 600px */


}

/* Container cho khóa học */
.course-container {
    background: #fff;
    border-radius: 15px;
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.5);
    padding: 25px;
    max-width: 1200px;
    width: 100%;
    height: 100%;
    margin-bottom: 20px;
    animation: slideIn 1s ease-in-out;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease-in-out;
}

.course-container:hover {
    transform: scale(1.02);
    box-shadow: 0 12px 25px rgba(0, 0, 0, 0.2);
}

@keyframes slideIn {
    0% {
        opacity: 0;
        transform: translateY(50px);
    }
    100% {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Các style khác giữ nguyên */
.row {
    display: flex;
    align-items: flex-start;
}

.col-lg-3 {
    flex: 0 0 25%;
    padding-right: 20px;
}

.col-lg-9 {
    flex: 0 0 75%;
}

/* Stylish button */
.study-now-btn {
    background: linear-gradient(45deg, #6a11cb, #2575fc);
    color: white;
    padding: 12px 25px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    font-size: 16px;
    font-weight: 500;
    text-transform: uppercase;
    transition: all 0.3s ease-in-out;
    position: relative;
    z-index: 1;
    overflow: hidden;
}

.study-now-btn:hover {
    background: linear-gradient(45deg, #2575fc, #6a11cb);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
}

.study-now-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 300%;
    height: 100%;
    background: rgba(255, 255, 255, 0.2);
    transform: skewX(-45deg);
    transition: all 0.4s ease-in-out;
    z-index: 0;
}

.study-now-btn:hover::before {
    left: 100%;
}

/* Course title */
.course-title {
    font-size: 26px;
    margin-top: 0;
    color: #444;
    font-weight: bold;
    transition: color 0.3s ease;
}

.course-title:hover {
    color: #a27e3b;
}

/* Modernized video */
.course-video {
    margin-top: 20px;
    border-radius: 10px;
    width: 800px;
    height: 400px;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease;
}

.course-video:hover {
    transform: scale(1.02);
}

/* Course description */
.course-description {
    margin-top: 15px;
    font-size: 18px;
    line-height: 1.7;
    color: #666;
    background-color: #f8f9fa;
    padding: 10px 15px;
    border-radius: 8px;
    transition: background-color 0.3s ease;
}

.course-description:hover {
    background-color: #eef1f5;
}
</style>
